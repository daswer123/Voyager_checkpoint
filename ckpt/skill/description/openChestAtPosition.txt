async function openChestAtPosition(bot) {
    // The function is about opening a chest at a specific position and checking its contents. It first moves the bot to the chest using the `moveToChest` function. Then, it opens the chest and waits for it to load. After that, it closes the chest and sends a chat message indicating that the chest has been opened and checked.
}